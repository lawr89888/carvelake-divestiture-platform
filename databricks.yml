bundle:
  name: carvelake-divestiture-platform

include:
  - resources/*.yml

variables:
  prefix:
    default: ""

targets:
  prod:
    default: true
    mode: production
    workspace:
      host: https://dbc-7a6b5c6f-4807.cloud.databricks.com
      root_path: /Workspace/Users/mo902138@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      prefix: ""